#include <gtest/gtest.h>

// GoogleTest provides its own main() via GTest::gtest_main, but we include
// this file so the test executable has a well-known translation unit that
// can be used for any global test-runner configuration in the future.
